{{attach_library('module_hero/hero-style')}}
<h3 class="animated bounce">{{title}}</h3>
<ol>
{% for item in items %}
    <li class="animated {{ random(['bounce','flash', 'rubberBand', 'shake', 'jello', 'tada', 'bounceIn']) }}">{{item.name}}</li>
{% endfor %}
</ol>
